package ru.abenefic.cloudvault.client.support;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

/**
 * автоматически выгружаем новые файлы на сервер
 */
public class UserFileWatcher {
    private static final Logger LOG = LogManager.getLogger(UserFileWatcher.class);

}
